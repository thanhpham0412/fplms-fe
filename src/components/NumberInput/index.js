import { Container, StyledInput } from './style';

const NumberInput = () => {
    return (
        <Container>
            <StyledInput></StyledInput>
        </Container>
    );
};

export default NumberInput;
